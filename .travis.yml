language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t pedrosato/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true pedrosato/docker-react npm run test

